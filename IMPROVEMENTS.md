# 浮光note改进清单

审计日期：2026-07-19

当前结论：现有视觉结构已经成立，不需要推倒重做。下一阶段优先解决内容真实性、交互完整度和正式上线准备。

## P0：发布前必须完成

- [x] 配置真实站点地址
  - 已改为通过 `SITE_URL` 配置，并保留 `http://127.0.0.1:4330` 作为本地回退值。
  - 仍需在正式发布前填入实际域名；canonical、Open Graph、RSS、sitemap 与 robots 会统一继承该地址。
  - 验收：生产构建中不再出现 `127.0.0.1`，所有绝对 URL 指向真实域名。（尚未正式发布，域名确定后按 `README.md` 部署章节设置 `SITE_URL` 重建即可）

## P1：优先改进

- [x] 替换真实作者资料和示例身份
  - 作者已改为 `Theologian25 · 愚者`，邮箱填入 `theologian25@qq.com`，关于页与页脚改为数据驱动读取 `src/data/site.ts`。
  - 首次人称文案与示例传记已核对，不再使用“林屿”身份。
  - 关于页首屏摄影图授权链接仍待补充（见 P2）。

- [x] 为文章准备语义一致的独立封面
  - 每篇文章由 `post-*` 独立封面承接，从既有素材裁切不同区域、宽高比与色调生成。
  - 分类图片只保留为 fallback，逐篇封面及替代文本描述对应场景。
  - 注意：封面由脚本从现有素材派生，视觉与原图同源；若需真正独立的场景照片，仍需逐篇替换为实拍或授权图片。

- [x] 用真实笔记替换占位内容
  - 将 Obsidian 笔记库（`E:\BaiduSyncdisk\Notes`，151 篇）中可公开的高质量笔记改写为博客文章。
  - 首期 3 篇：《为什么你攒不下钱？》读书批注（reading）、LitCTF EasySql 宽字节注入复盘（tech）、ArchWSL 图形化配置指南（tech）。
  - 删除虚构占位文章：AI 工作流、内容安全、本地优先、主题系统、城市步行地图、晚饭、雨天工作台 7 篇。
  - 安全红线：改写 ArchWSL 时删除笔记中真实密码，全部替换为占位提示。
  - 站点文案（site.ts 描述、moments 时间线、about 页、首页 NOW BUILDING）同步去占位化，定位改为"安全技术、CTF 复盘与读书笔记"。
  - 剩余占位：`slow-query-checkup`（featured）、《禅与摩托车维修艺术》、《Outer Wilds》仍为演示内容，待后续用真实笔记替换。

- [x] 将首页文章数据移入 frontmatter
  - `ISSUE 07`、季节、地点、读书编号、引语、游戏状态、时长和评分目前写死在首页模板中。
  - 建议增加可选字段：`issue`、`location`、`quote`、`gameStatus`、`playtime`、`rating`。
  - 验收：更换首页最新文章后，不会继承上一篇文章的数据。

- [x] 自动计算阅读时长
  - 当前正文约 143–580 个有效字符，却手填为 4–10 分钟。
  - 按中文字符和英文词数在构建阶段计算，允许少数文章手动覆盖。

- [x] 修复短屏和横屏搜索弹窗
  - 弹窗使用 `overflow: hidden`，结果区又有固定最大高度。
  - 实测 `844×390` 视口下后半结果被裁切。
  - 验收：弹窗本身不超出视口，结果区可独立滚动，键盘焦点始终可见。

- [x] 提升浅色模式文字对比度
  - 原珊瑚色小字对背景约为 `2.37:1`，低于 WCAG AA 的 `4.5:1`。
  - 灰色辅助文字约为 `4.15:1`。
  - 为浅色模式增加更深的文字型 accent，装饰色继续用于大面积或非文字元素。

- [x] 主强调色由珊瑚橙替换为金色
  - 全局强调色 `--coral` / `--coral-text` 更名并替换为金色 `--gold` / `--gold-text`
    （浅色 `#d8a73e` / 文字 `#8a6a1f`，深色文字 `#e7c15c`）。
  - `life` 分类 accent 与 `favicon.svg` 同步改为金色。
  - 全站样式、`site.ts`、图标中已无珊瑚橙残留。

- [x] 通用强调色改青色系，life 分类保留金色
  - 早期把通用强调色一并改为金色，导致全站通用元素（导航、hero、时间线、
    signoff、about、搜索高亮）与 life 分类撞色、整体偏金。
  - 新增青色系通用强调色 `--accent`（浅色 `#1e9c66`/文字 `#166b45`，
    深色 `#2fd393`/文字 `#4fe6a5`，对比度 ≥4.5:1）。
  - 14 处通用元素改 `--accent`，3 处 life 专属元素（搜索 life 标签、
    life 区块装饰条、life-notes）保留 `--gold`。
  - 四分类 accent 各自独立：tech 青绿 / life 金 / reading 红 / gaming 黄。

## P2：功能与维护

- [x] 将主题切换改为“系统 / 浅色 / 深色”三态
  - 当前首次访问会跟随系统，但手动切换后无法恢复自动模式。
  - 同步更新按钮名称、当前状态和主题色元数据。

- [x] 修复移动菜单跨断点状态
  - 移动菜单打开后扩大到桌面宽度，会与桌面导航同时显示。
  - 桌面断点应强制关闭移动菜单，并为两套导航增加 `aria-current="page"`。

- [x] 补齐搜索弹窗无障碍语义
  - 为 dialog 添加可访问名称。
  - 为输入框提供清晰的焦点态。
  - 将结果数量设置为 `aria-live="polite"` 或 `role="status"`。

- [x] 为首页分类增加空状态
  - 当前首页假定四个分类始终都有文章。
  - 删除分类唯一文章或全部设为草稿时会导致构建失败。
  - 验收：空分类显示合理占位，或在内容校验阶段给出清晰错误。

- [x] 调整移动端首页长度
  - `390px` 宽度下首页约 `8212px` 高。
  - “最近更新”与主推、分类模块重复，可缩减为 3–4 篇或排除已展示文章。

- [x] 升级搜索实现
  - 已改为独立索引:构建时输出唯一一份 `/search-index.json`,客户端首次搜索时
    懒加载并在内存建倒排索引(中文 bigram + unigram,英文按词),按字段加权排序。
  - 搜索数据不再内联到每页(此前每页重复嵌入全文,呈 O(N²) 膨胀);单页 HTML
    实测下降约 40–65%,且随文章增多单页体积保持恒定。
  - 单字中文查询也能命中(unigram 兜底)。更大规模(数千篇)可再迁移到 Pagefind。

- [x] 提供移动端文章目录
  - `920px` 以下在正文开头显示可折叠目录，桌面端继续使用粘性侧栏。

## P2：性能与发布

- [x] 生成响应式图片
  - 为 9 组素材生成 480 / 960 / 1600 三档 WebP。
  - 所有页面图片均提供 `srcset`、`sizes`、`width`、`height` 和替代文本。

- [x] 将原图移出 `public`
  - 已清理 `public/images/source-*` 的 6 个旧副本，原图统一保存在 `assets/sources/`。
  - 构建产物由约 `8.04MB` 降至 `5.25MB`，减少约 `2.79MB`。

- [x] 让项目可独立安装
  - 已补 Node 版本约束、`packageManager` 和 `sharp` 依赖声明。
  - 已在干净目录用 `npm ci` 验证安装（292 包）、`prepare:images` 与 `astro check`+`astro build` 全流程通过，不再依赖目录联接。
  - 验收：在干净目录使用锁定安装后可完成检查、构建和图片处理。（已通过）

- [x] 改用正式静态部署
  - `astro preview` 只适合本地预览，当前资源均返回 `Cache-Control: no-cache`。
  - 已新增 `public/_headers`（Netlify / Cloudflare Pages 通用）：HTML 不缓存、`_astro/` 带哈希资源长缓存、图片短缓存、feed / sitemap / robots 一小时缓存。
  - 已部署到 GitHub Pages：https://wrhyija1379.github.io/fuguang-notes/（source=gh-pages 分支，`build_type: legacy`）。
  - 子路径部署适配：`astro.config.mjs` 支持 `SITE_BASE`；RSS / sitemap / robots / canonical / og:image / 文章链接 / 搜索结果全部按 `base` 前缀生成，避免指向域名根。
  - 部署通过 Git Data API 完成（本机 github.com 被 DNS 劫持，走 `api.github.com` 直连 IP）；可部署 `dist` 到任意静态托管或 CDN。
  - 同时补充了 404 页面与 `README.md` 部署章节。

- [x] 补齐搜索引擎和分享元数据
  - 添加 sitemap、`robots.txt` 和 RSS 自动发现链接。
  - 文章页使用 `og:type=article`，补充发布日期、更新时间、作者、Twitter Card 和 `BlogPosting` JSON-LD。
  - RSS 增加语言、更新时间、分类和自引用链接。

- [ ] 替换或确认关于页图片授权
  - 已将旧模板的抽象素材替换为钢笔摄影图。
  - 当前仍需补回摄影者和 Unsplash 原始页面链接后再公开发布。

## 推荐实施顺序

1. 真实作者资料、逐篇封面、动态文章数据、自动阅读时长。
2. 搜索弹窗、颜色对比、三态主题、移动导航和空分类状态。
3. 响应式图片、独立依赖、真实域名、sitemap 与结构化数据。

## 当前基线

- 14 个静态页面构建通过，`0 errors / 0 warnings / 0 hints`（含 404 页）。
- 桌面和常规手机视口无横向溢出，图片与内部链接正常。
- 控制台无运行时错误，RSS 返回 `200`。
- CSS 与客户端 JavaScript gzip 后合计约 `13.9KB`。
- 文章数量 6 篇：技术 `2`（真实）/ 读书 `2`（1 真实）/ 游戏 `1`（占位）/ 生活 `0`。
- 全部 6 篇文章具备独立封面；作者资料为 `Theologian25 · 愚者`。
- 四分类色：tech 墨绿 / life 金 / reading 红 / gaming 紫；通用强调色月白。
- 已部署 GitHub Pages：https://wrhyija1379.github.io/fuguang-notes/（status: built）。
